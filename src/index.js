import React from "react";
import ReactDOM from "react-dom";

import PreviewGenerator from "./PreviewGenerator";

const rootElement = document.getElementById("root");
ReactDOM.render(<PreviewGenerator />, rootElement);
